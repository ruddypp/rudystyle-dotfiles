[Settings]
# Sengaja kosong.
# Semua setelan tampilan = per-kamar, lewat dconf:
#   GNOME    -> ~/.config/dconf/user
#   Hyprland -> ~/.config/dconf/hypr  (DCONF_PROFILE di hyprland.conf)
# Jangan taruh gtk-theme-name / gtk-font-name / gtk-application-prefer-dark-theme
# di sini: file ini dibaca dua sesi, nilainya bakal bocor antar kamar.
